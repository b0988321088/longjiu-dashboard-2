# 信用卡口徑評估（P1-4）— 2026-09-13

> 用途：動 snapshot 卡片數字（P2）前的影響評估。**本文件只做診斷，未改任何數字。**
> 起因：INC-159 P0-1 修好預算報告資料源後，發現同一組「信用卡」在系統內存在多個口徑與來源且彼此不一致。

## 一、現況：同一組信用卡，6 個不同數字

| # | 位置 | 值（合計） | 口徑／來源 | 資料日 |
|---|------|-----------|-----------|--------|
| 1 | `snapshot.credit_card`（dict） | 66,999（負值合計） | 帳戶 CSV「卡」帳戶負值 | 8/20 期 |
| 2 | `snapshot.credit_card_pending` | 66,699 | 同上（與 #1 差 300，未解釋） | 8/20 期 |
| 3 | `snapshot.cc_liability` | 28,101 | 來源不明（無程式寫入點，疑手寫） | 不明 |
| 4 | `snapshot.cc_unbilled`（dict） | 32,502 | **帳單 CSV 最新一期金額** | **9/02 ✅最新** |
| 5 | `dragon_assets.db → liabilities.credit_card` | 71,799 | 9/8 寫入，寫入者非現行 `db_loader.py`（其預設值為 39,865） | 9/08 |
| 6 | 預算報告（`BUDGET_WEEKLY_REPORT.md`） | 四卡循環 15,793／其他卡 18,232 | 帳戶 CSV 負值（P0-1 已改讀 9/02） | 9/02 |
| 7 | `snapshot.total_liabilities` 內的卡片成分 | **78,099（未標示）** | 由殘差反推（見下節） | — |

同一時點的金額差異區間 **15,793 → 78,099（約 5 倍）**，主因是口徑混用：
- 「當期循環未繳」（本月要繳的錢，帳戶 CSV 負值）
- 「最新一期帳單金額」（帳單已出帳金額）
- 「全部卡含未出帳／分期」（較大，含國泰 CUBE 等未列卡）

## 二、`total_liabilities` 對帳缺口

`snapshot.total_liabilities = 30,160,643`，可標示的組成：

| 項目 | 金額 |
|---|---|
| 永豐 3 筆房貸 | 13,082,544 |
| 國泰房貸（大義街 1,200 萬） | 12,000,000 |
| 保單借貸（安聯 A/B + 第一金） | 4,000,000 |
| 券商質押 | 1,000,000 |
| **小計** | **30,082,544** |
| **殘差（未標示）** | **78,099** |

殘差 78,099 與任何既有卡片欄位都不吻合（66,699／28,101／71,799 皆非），且 `personal_loans.女友借款 300,000` 未計入。
→ **結論：目前無法從 snapshot 欄位完整重建總負債**；在對帳完成前，任何「改卡片口徑 → 淨值變動 X」的聲明都不可靠。

## 三、下游影響面（若改卡片數字會動到什麼）

- 讀 `total_liabilities` 者：`asset_diff_monitor`（差異分析）、`monthly_report`、`build_ceo_dashboard`、`build_audit_dashboard`、`build_weekly_report`、`notion_weekly_trend_wrapper`、`dragon_assets.db`
- 讀 `cc_liability` 者：`run_daily`（日報）
- 讀 `liabilities.credit_card`（DB）者：`dynamic_review`（週日週報）
- 結論：**卡片欄位是「多點消費、多點手寫」**，任何單點更新都會造成新一輪不一致。

## 四、建議（P2 執行前的前置作業）

1. **口徑定版**（建議）：保留兩欄語意並明確命名
   - `cc_cycle_due`＝當期循環未繳（帳戶 CSV 負值合計，本月現金流出）
   - `cc_unbilled`＝未出帳（帳單 CSV 最新一期）
   - `cc_liability_total`＝上兩者合計（進總負債用）
2. **單一寫入者**：三個欄位只由 `update_data.py --moneybook` 一個入口寫入；`cc_liability`／`credit_card_pending` 改為衍生值（禁手寫）；DB `liabilities.credit_card` 由 snapshot 衍生
3. **先對帳 78,099**：確認是卡片、循環息或其他（需使用者提供一張信用卡總覽截圖即可一次定案）
4. **同步後驗證**：四源一致（snapshot→DB→HTML→asset_diff）＋差異分析前後比對表；只覆蓋卡片欄位，**不動 9/9 現金源**
5. **風險**：淨值敏感度 = Δ卡片負債（保守估 30-60 萬區間，需對帳後才能給準數字）

## 五、本次不動的原因

- 未對帳即改 → 可能把「口徑修正」誤植成「負債變少／淨值變好」，影響日報、差異分析與 Notion 歷史趨勢
- 建議先做第四節 1-3，再由使用者核准套用
