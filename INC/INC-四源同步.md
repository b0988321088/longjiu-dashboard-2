# INC-四源同步（2026-07-28）

## 症狀
- 四源不一致長達 2 小時
- FL65 第一金用 snapshot 舊值 1,977,799 覆蓋日報正確值 1,958,980
- Git push 失敗、Pages 快取未更新

## 根因
1. snapshot 為舊值，日報早已校正為新值，但 sync 腳本以 snapshot 為唯一真值
2. patch over patch：同一問題改 4 次才解決
3. 沒有先給使用者核對數字就執行同步

## 對策
- **先核對再同步**：跑 sync 前先列關鍵數字給使用者確認
- **使用者核准的數字才是真值**，不是 snapshot
- 鐵則已寫入 `data-pipeline-automation` 技能

## 狀態
✅ 已關閉（2026-07-28 03:31）
